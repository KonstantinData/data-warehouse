# Temporary Outputs

This directory is reserved for ephemeral, local-only artifacts.

- Do not commit files from `tmp/`.
- Use for transient analysis outputs, debugging data, or prompt runs.
- Clean up regularly to avoid stale artifacts.
