# Prompt Catalog

This directory contains curated prompts used by agentic steps (drafting, building, and reviews).

- Prompts are inputs to LLM-driven agents in `src/agents/`.
- Treat prompt text as versioned source; changes should be reviewed like code.
- Do not store outputs or run logs here.
